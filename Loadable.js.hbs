import React from 'react';
import loadable from 'utils/loadable';
import {ScreenLoadingIndicator} from '../../../components/LoadingIndicator';

export default loadable(() => import('./index'), {
	fallback: <ScreenLoadingIndicator />,
});
